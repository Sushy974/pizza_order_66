import 'package:flutter/material.dart';

class IncriptionView extends StatelessWidget {
  const IncriptionView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
